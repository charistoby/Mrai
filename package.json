{
  "name": "mr-ai",
  "version": "2.0.0",
  "description": "MR.AI Maths Tutor v2",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "start": "node dist/server.cjs",
    "lint": "echo 'No linting needed'"
  },
  "dependencies": {
    "@google/genai": "^2.11.0",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.1.1",
    "esbuild": "^0.28.1",
    "tsx": "^4.23.1"
  }
}
